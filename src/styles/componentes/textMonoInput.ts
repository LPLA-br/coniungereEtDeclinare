import { StyleSheet } from "react-native";

const estiloTextMonoInput = StyleSheet.create(
  {
    componentesEmLinha:
    {
      display: 'flex',
      flexDirection: 'row',
    }
  }
);

export default estiloTextMonoInput;
