import { StyleSheet } from "react-native";

const estiloDeclinacoes = StyleSheet.create(
  {
    singPlur:
    {
      display: 'flex',
      flexDirection: 'row',
      justifyContent: "space-between",
    },
    entradas:
    {
      margin: 10
    }
  }
);

export default estiloDeclinacoes;
