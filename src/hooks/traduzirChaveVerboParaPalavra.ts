import VerbObj from "../constants/VerbObj";

/** Api tem endpoints a nível de modo
 *  frontend tem opções a nível de tempo.
 *  Para não haver requisições inúteis
 *  considera-se: deve haver 1..N
 *  tempo(s) por modo para que
 *  a requisição para o modo seja feita.
*   NÃO IMPLEMENTADO
* */
const controlarRequisicoesVerbos = ( chave: keyof VerbObj ): string =>
{
  return "";
}

